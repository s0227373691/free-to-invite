import styled from "styled-components";

const Button = styled.button`
    background: white;
    outline-style: none;
    border: 0px;
    outline-style: none;

    &:hover {
        cursor: pointer;
    }
`;

export default Button;
