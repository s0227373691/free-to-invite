import styled from 'styled-components';

export const SelectClearDefault = styled.select`
    padding: 10px;
    text-align-last: center;
    border-radius: 5px;
    border: 1px solid #dadce0;

    &:focus {
        outline: none;
    }
`;
