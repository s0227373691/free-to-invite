import styled from 'styled-components';

export const TextareaClearDefault = styled.textarea`
    padding: 0;
    border-radius: 5px;
    border: 1px solid #dadce0;
    resize: none;

    &:focus {
        outline: none;
    }
`;
